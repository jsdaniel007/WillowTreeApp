#!/usr/bin/env python
import openpyxl
import os

""" Purpose: To create product inventory system data through excel sheet formatting,
having the excel sheets essentially become the database
Major actions should include:
- Create a Transaction row
- Edit an existing Transaction row
- Delete a Transaction Row

"""

# Create a line on the row, changing behavior depending on the ScreenID given?
def addRow():
	pass

# Create a line on the row, changing behavior depending on the ScreenID given?
def editRow():
	pass


def delRow():
	pass
